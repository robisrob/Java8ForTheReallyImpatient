apply plugin: 'java'

sourceCompatibility = 1.5
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    def assertjVersion = '3.1.0'
    def jUnitVersion = '4.11'

    testCompile(
        'junit:junit:' + jUnitVersion,
        'org.assertj:assertj-core:'+ assertjVersion
    )

    compile(

    )
}